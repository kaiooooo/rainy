function funcao(param) {
  //bloco de codigo
}

funcao("param");

//expressao de funcao
const soma = function (num1, num2) {
  return num1 + num2;
};
//console.log(soma(1, 1));

//diferenca principal
function apresentar() {
  return "olá";
}
